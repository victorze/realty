extends ../layout

block content
  div(class="py-10")
    h1(class="my-10 text-4xl text-center font-extrabold")
      | Bienes
      span(class="font-normal")
        | #{' '} Raices
    h2(class="text-2xl text-center font-extrabold")
      | Crear una cuenta

    div(class="mt-8 mx-auto max-w-sm")
      div(class="bg-white py-8 px-4 shadow")
        form(class="space-y-5 px-4" method='POST' action='/auth/signup')
          input(
            class="w-full border border-gray-300 rounded px-3 py-1 outline-none placeholder-gray-500 focus:border-indigo-500"
            name="name"
            placeholder="Nombre"
          )
          input(
            class="w-full border border-gray-300 rounded px-3 py-1 outline-none placeholder-gray-500 focus:border-indigo-500"
            name="email"
            type="email"
            placeholder="Correo electrónico"
          )
          input(
            class="w-full border border-gray-300 rounded px-3 py-1 outline-none placeholder-gray-500 focus:border-indigo-500"
            name="password"
            type="password"
            placeholder="Contraseña"
          )
          input(
            class="bg-indigo-600 w-full rounded py-2 hover:bg-indigo-700 text-white text-sm font-bold cursor-pointer"
            type="submit"
            value="Regístrate"
          )

      div(class="bg-white mt-4 py-6 px-8 shadow text-center text-sm")
        p ¿Tienes una cuenta?
          a(
            class="text-indigo-500 hover:text-indigo-600"
            href="/auth/login"
          ) #{' '} Inicia sesión
