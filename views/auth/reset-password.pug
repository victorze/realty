extends ../layout/index

block vars
  - var pageTitle = "Crea una contraseña nueva"

block content
  +title(pageTitle)

  div(class="mx-auto mt-8 max-w-sm")
    div(class="bg-white px-8 py-8 shadow")
      form(
        class="flex flex-col gap-y-5"
        method="POST"
        noValidate
      )
        +csrf
        div
          +input('password')(
            type="password"
            placeholder="Contraseña nueva"
          )
        div
          +input('confirmPassword')(
            type="password"
            placeholder="Repetir contraseña nueva"
          )
        div
          +submit('Restablecer contraseña')(class="w-full")
