extends ../layout/index

block vars
  - var pageTitle='Cuenta verificada'

block content
  div(class="pt-20 text-center")
    h1(class="mb-4 text-slate-800 text-2xl font-bold")= pageTitle
    p(class="mb-4 text-slate-800 text-lg") !Felicidades! Tu cuenta ha sido verificada.
    +link('/auth/login', 'Iniciar sessión')
